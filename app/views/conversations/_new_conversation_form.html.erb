<%= form_with(model: @new_conversation, url: conversations_path, data: { turbo_frame: "new-conversation-form" }) do |form| %>
  <!-- Your form fields here -->
  <%= form.submit "Create", class: "bg-blue-500 text-white py-2 px-4 rounded-md mt-2" %>
<% end %>
